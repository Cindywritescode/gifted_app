<h1>Your Friends</h1>

<% @friends.each do |friend| %>
  <ul>
    <li>
      <%= friend.name %> |
      <%= link_to 'delete', friend_path(friend), method: :delete, data: { confirm: "Are you sure you want to delete #{friend.name} from your friend list?" } %>
    </li>
  </ul>
<% end %>
